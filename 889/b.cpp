#include <bits/stdc++.h>
using namespace std;

long long n;
long long solve() {
    cin >> n;
    if (n % 2 == 1) return 1;
    else if (n % 2 == 0) {
        if (n % 3 != 0) return 
    }
}

int main() {
    int t; cin >> t;
    while (t--) cout << solve() << '\n';
}