#include <bits/stdc++.h>
using namespace std;

vector<int> nums;

void solve() {
    int n, k; cin >> n >> k;

    nums = vector<int>()

    for (int i = 0; i < n; i++) {
        int d; cin >> d;
        nums[d] = 1;
    }
}


int main() {
    int t; cin >> t;
    while (t--) solve();
}